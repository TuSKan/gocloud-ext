# This file lists all the modules in this repository; it's used by CI to
# perform operations for each module. The format matches go-cloud's own
# allmodules file.
#
# Any line that doesn't begin with a '#' character and isn't empty is treated
# as a path relative to the top of the repository that has a module in it.
# The 'released' field specifies whether this is a module we tag and publish.

# module-directory           released
.                            yes
blob/httpblob                yes
blob/sftpblob                yes
blob/multipart               yes
blob/multipart/s3mp          yes
